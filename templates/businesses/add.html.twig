{# business_form.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Add your business{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="form_container">
                    <h1>Add Business</h1>
                    {{ form_start(form) }}
                        <div class="form-group">
                            <label for="name" class="form-label">Name</label>
                            {{ form_widget(form.Name, {'attr': {'class': 'form-control'}}) }}
                        </div>
                        <div class="form-group">
                            <label for="description" class="form-label">Description</label>
                            {{ form_widget(form.Description, {'attr': {'class': 'form-control'}}) }}
                        </div>
                        
                        <div class="form-group">
                            <label for="country" class="form-label">Country</label>
                            {{ form_row(form.Country, {'attr': {'class': 'form-control'}}) }}
                        </div>
                         
                        <div class="form-group">
                            <label for="revenue" class="form-label">Revenue</label>
                            {{ form_widget(form.Revenue, {'attr': {'class': 'form-control'}}) }}
                        </div>
                        <div class="form-group">
                            <label for="profit" class="form-label">Profit</label>
                            {{ form_widget(form.Profit, {'attr': {'class': 'form-control'}}) }}
                        </div>
                        
                        <div class="form-group">
                            <label for="imageBlob" class="form-label">Business Image</label>
                            {{ form_widget(form.ImageBlob, {'attr': {'class': 'form-control'}}) }}
                        </div>
                        
                        <div class="form-group">
                            <button type="submit" class="btn btn-primary">Submit</button>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
